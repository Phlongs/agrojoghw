<% include index %> 
	<center>
	<br>
	<br>
	<br>

	<h1> Edit Game: </h1>

	<form method="post" action="/editGame/<%= game._id %>" enctype="multipart/form-data">

		<label>Edit Your Game Title</label><br>
		<input type="text" name="gameTitle" value="<%= game.gameTitle %>" required>
		<br><br>

		<label>Edit Publisher</label><br>
		<%= game.gamePublisher %>
		<select name="gamePublisher">
		 	<option value="<%= game.gamePublisher %>" >Select</option>
			<% publishers.forEach(function(publisher) { %>
			<option value="<%= publisher.publisher %>"><%= publisher.publisher %></option>
			<% }); %>
		</select>
		<br><br>

		 	<label>Change Image</label><br><img src="../uploads/<%= game.gameImage %>" alt="" style="width:20px;height:20px;">
			<input type="file" name="gameImage" value="<%= game.gameImage %>">
		<br><br>

		<input type="submit" value="Submit">
	</form>

	<form method="delete" action="/deleteGame/<%= game._id %>">
	<input type="submit" value="Delete">
	</form>

	</center>

